apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: production-cicd-cluster
  region: eu-north-1

iam:
  withOIDC: true

managedNodeGroups:
  - name: production-cicd-nodes
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 2